# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

#borramos lo que habia para ajustarlo a lo nuestro

#para probar nuestro controlador. La ruta sale del archivo ControladorReserva en el package, y añadimos los puntos para obtener el metodo

GET /api/reservas/:id   reservas.infraestructura.controladores.ControladorReserva.obtenerReserva(id: String)

#luego vamos a la terminal, escribimos sbt compile y luego sbt run, si pide usar el servidor por defecto damos yes
#luegpos podemos ir a postam y probar el código. De momento me lanza pero lo tumba a la vez

POST /api/reservas/   reservas.infraestructura.controladores.ControladorReserva.crearReserva
#pasamos ya con json en postman el crear un post de reserva



